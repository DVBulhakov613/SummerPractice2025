<UserControl x:Class="OOP_CourseProject.Controls.TopPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBackgroundBrush}">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,5">
            <TextBox Width="250" Height="25" Margin="5"/>
            <TextBox Width="300" Height="25" Margin="5"/>
            <Border Background="{StaticResource SidePanelBackgroundBrush}" 
                    CornerRadius="5" Padding="5" Margin="5" VerticalAlignment="Center">
                <TextBlock Foreground="White" FontWeight="Bold" Text="Placeholder"/>
            </Border>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5,0,5">
            <Button Width="40" Height="40" Margin="5" Background="{StaticResource AccentBrush}" />
            <Button Width="40" Height="40" Margin="5" Background="{StaticResource AccentBrush}" />
            <Button Width="40" Height="40" Margin="5" Background="{StaticResource AccentBrush}" />
        </StackPanel>

    </Grid>
</UserControl>
